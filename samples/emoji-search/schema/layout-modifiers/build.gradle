apply plugin: 'org.jetbrains.kotlin.multiplatform'
apply plugin: 'app.cash.redwood.generator.layoutmodifiers'

archivesBaseName = 'schema-layout-modifiers'

kotlin {
  iosArm64()
  iosX64()
  iosSimulatorArm64()

  js {
    browser()
  }

  jvm()

  sourceSets {
    commonMain {
      dependencies {
        api projects.redwoodLayoutApi
      }
    }
  }
}

redwoodSchema {
  source = projects.samples.emojiSearch.schema
  type = 'example.schema.EmojiSearch'
}
